# Permanent diagnosticeringsværktøj — se services.py. Ikke en del af normal drift.
export_meter_data:
  name: Brunata — Eksportér rå målerdata (debug)
  description: >-
    Diagnosticeringsværktøj. Henter statistics_during_period() direkte for én
    entity i et givet datointerval — helt uafhængigt af dashboard-kortet — og
    skriver det RÅ resultat til /config/brunata_debug_query.json (overskrives
    hver gang). Bruges manuelt til fejlsøgning, ikke af integrationen selv.
  fields:
    entity_id:
      name: Entity ID
      description: >-
        Den fulde entity_id, fx sensor.brunata_varmt_vand_entre — ikke et
        meter_id, og ikke navnet ("Varmt vand Entre").
      required: true
      example: "sensor.brunata_varmt_vand_entre"
      selector:
        text:
    start_date:
      name: Startdato
      description: Startdato for intervallet (inklusiv), format ÅÅÅÅ-MM-DD.
      required: true
      example: "2026-07-01"
      selector:
        text:
    end_date:
      name: Slutdato
      description: Slutdato for intervallet (eksklusiv), format ÅÅÅÅ-MM-DD.
      required: true
      example: "2026-08-01"
      selector:
        text:
    period:
      name: Periode
      description: Samme "period"-parameter som statistics_during_period() selv bruger.
      required: false
      default: month
      selector:
        select:
          options:
            - "5minute"
            - "hour"
            - "day"
            - "week"
            - "month"

reconcile_recorder_statistics:
  name: Brunata — Ret recorder-statistik fra ledger (manuel)
  description: >-
    Genberegner én målers langtidsstatistik i HA's recorder ud fra
    integrationens egen ledger-fil (uafhængig af recorder'ens "sum"-kolonne)
    og retter den via async_import_statistics() — kun for allerede
    afsluttede timer, aldrig den igangværende. Ikke koblet ind i normal
    drift; kaldes kun manuelt herfra.
  fields:
    entity_id:
      name: Entity ID
      description: >-
        Den fulde entity_id, fx sensor.brunata_varmt_vand_entre — ikke et
        meter_id, og ikke navnet ("Varmt vand Entre").
      required: true
      example: "sensor.brunata_varmt_vand_entre"
      selector:
        text:
